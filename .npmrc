; netlify-cli pins an @opentelemetry/api that conflicts with vitest's optional
; peer dependency, which breaks `npm install` (ERESOLVE) on Netlify. Resolve
; peers the old, lenient way so CI installs succeed.
legacy-peer-deps=true
